[pytest]
# Tests import `pipeline` and `source_registry` from the repo root. Bare
# `pytest` does not put the root on sys.path (only `python -m pytest` does),
# so state it explicitly and let both invocations work.
pythonpath = .
testpaths = tests
