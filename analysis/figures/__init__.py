"""Measurement of the figure guard (`validate.assert_figures_are_sourced`).

Measurement only, never collection: nothing here fetches, classifies or writes
a row. See `replay.py` for what can and cannot be measured from what we store.
"""
